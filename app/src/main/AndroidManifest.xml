<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.railapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--*************** Ami ***********************-->

        <activity android:name=".PassengersListActivity">
            <!-- Put Intent filter here -->
        </activity>
        <activity android:name=".PassengerDetailsActivity" />

        <!--******************** Monika *****************-->

        <activity android:name=".MainActivity" >
            <!-- Put Intent filter here -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ListOfTrains_Recycle_Activity" />
        <activity android:name=".Update_trains_Activity" />

        <!--*************** Khushal *************************-->
        <activity android:name=".Train_All_List_Activity">
            <!-- Put Intent filter here -->


        </activity>
        <activity android:name=".Stations_List_Activity" />
        <activity android:name=".RouteListActivity" />

    </application>

</manifest>